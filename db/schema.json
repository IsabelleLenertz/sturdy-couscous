{ 
    "URL":"https://github.com/IsabelleLenertz/sturdy-couscous",
    "Title":"sturdy-couscous",
    "Domain":"Github",
    "Checker":{
        "tls_versions_supported": [ "TLSv1.3", "TLSv1.2", "TLSv1.1", "TLSv1.0"],
        "open_ports":["list of int"],
        "certificate_valid":true,
        "ciphers_supported": {
            "TLSv1.0":["list of strings"],
            "TLSv1.1":["list of strings"],
            "TLSv1.2":["list of strings"],
            "TLSv1.3":["list of strings"]
    },
    "Classification":{
        "Categories": ["social" "media", "IT/dev"],
        "Data": {
            "Keywords": ["list of strings"],
            "Extention":".com" //(nullable)
        }
    }
//SECURITY CRAWLER
    // Create a thread for a given url 
        // wihtin that thread:    
        // call classifier and wait for results
        // call checker and wait for results
        // store all that in mongo
    // when all threads are done, call printer


// PRINTER
    // proportion of website that support a TLS version
    // that belong to a specific category